describe('SessionService', function(){
	beforeEach(module('ppApp'));

	xit('sets a token when given proper credentials', function(){
		inject(function(){

		});
	});

	xit('does something when credentials are invalid', function(){

	});
});